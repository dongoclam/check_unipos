<div class="col-md-12">
   <div class="card">
      <div class="card-header card-header-info">
         <h4 class="card-title ">Unipos <strong>(<%= @count %>)</strong></h4>
         <p class="card-category">Total uniposes cloned from Framgia</p>
         <%= button_tag id: "clone-unipos", class: "btn btn-primary btn-simple" do %>
           <i class="material-icons">cloud_download</i> Clone
         <% end %>
      </div>
      <div class="card-body">
         <div class="table-responsive">
           <%= render "table" %>
         </div>
      </div>
   </div>
</div>

<%= render "shared/modal_detail" %>
<%= render "shared/modal_confirm" %>
<%= render "shared/modal_loading" %>
