<div class="col-md-12">
   <div class="card">
      <div class="card-header card-header-primary">
         <h4 class="card-title ">Chatwork Users</h4>
         <p class="card-category">All users are from Framgia HN</p>
         <button type="button" class="btn btn-primary load-user-from-chatwork">
           <i class="material-icons ">autorenew</i>Update
         </button>
      </div>
      <div class="card-body">
         <div class="table-responsive">
           <%= render "table" %>
         </div>
      </div>
   </div>
</div>

<!-- Modal -->
<%= render "modal_detail" %>
<%= render "modal_confirm" %>
<%= render "modal_loading" %>
